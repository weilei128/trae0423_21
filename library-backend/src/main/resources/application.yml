server:
  port: 10021

spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://49.235.161.106:10011/db1?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: wl
    password: Wl1733811
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

  data:
    redis:
      host: 49.235.161.106
      port: 10012
      password:

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.library.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

library:
  borrow:
    default-days: 30
    max-borrow-count: 10
    renew-days: 30
  fine:
    per-day-fine: 0.5
